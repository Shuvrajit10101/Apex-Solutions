namespace Apex.Desktop.ViewModels;

public partial class MainWindowViewModel : ViewModelBase
{
    // Phase-0 placeholder shell. The single-window Gateway of Tally UI (keyboard-first,
    // pixel-level fidelity) is built in later phases over the Apex.Ledger core.
    public string Greeting { get; } = "Apex Solutions — Tally Prime clone (Phase 0 shell)";
}
